name: Linear Weekly Update

on:
  schedule:
    # Monday 9 AM IST = Monday 3:30 AM UTC
    - cron: "30 3 * * 1"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (print payload, don't post)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

jobs:
  post-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python linear_weekly_update.py --dry-run
          else
            python linear_weekly_update.py
          fi
        env:
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
          LINEAR_TEAM_KEY: ${{ secrets.LINEAR_TEAM_KEY }}
          LINEAR_DISCORD_WEBHOOK: ${{ secrets.LINEAR_DISCORD_WEBHOOK }}
